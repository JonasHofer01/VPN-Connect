name: Build & Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Smoke test (syntax)
        run: python -m py_compile vpn_connect.py

      - name: Import test
        run: python -c "import vpn_connect; print('import ok')"

      - name: Build EXE
        run: |
          pyinstaller --onefile --windowed --name VPN_Connect --uac-admin --icon assets\app_icon.ico --runtime-tmpdir "C:\ProgramData\VPNConnect" vpn_connect.py

      - name: Build Installer (Inno Setup)
        run: |
          iscc /DAppVersion="${{ github.ref_name }}" VPN_Connect.iss

      - name: Generate SHA256
        run: |
          $hash_exe = (Get-FileHash -Algorithm SHA256 "dist\VPN_Connect.exe").Hash.ToLowerInvariant()
          Set-Content -Path "dist\VPN_Connect.exe.sha256" -Value "$hash_exe  VPN_Connect.exe" -Encoding ASCII
          $hash_setup = (Get-FileHash -Algorithm SHA256 "dist\VPN_Connect_Setup.exe").Hash.ToLowerInvariant()
          Set-Content -Path "dist\VPN_Connect_Setup.exe.sha256" -Value "$hash_setup  VPN_Connect_Setup.exe" -Encoding ASCII

      - name: Upload EXE as artifact
        uses: actions/upload-artifact@v4
        with:
          name: VPN_Connect
          path: |
            dist/VPN_Connect.exe
            dist/VPN_Connect.exe.sha256
            dist/VPN_Connect_Setup.exe
            dist/VPN_Connect_Setup.exe.sha256

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/VPN_Connect.exe
            dist/VPN_Connect.exe.sha256
            dist/VPN_Connect_Setup.exe
            dist/VPN_Connect_Setup.exe.sha256
          generate_release_notes: true

